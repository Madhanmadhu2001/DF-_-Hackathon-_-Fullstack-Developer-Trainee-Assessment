# Backend (DF Hackathon)

## Setup

1. Copy `.env.example` to `.env` and fill values.
2. Install deps: `npm install`
3. Create MySQL database `df_hackathon_d` or let Sequelize create tables.
4. Run: `npm run dev`
